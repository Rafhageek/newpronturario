name: Android preview (APK)

# Disparo manual (não constrói a cada push — economiza minutos de build).
on:
  workflow_dispatch: {}

jobs:
  build_android_preview:
    name: APK preview
    type: build
    params:
      platform: android
      profile: preview
